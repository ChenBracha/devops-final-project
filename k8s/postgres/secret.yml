apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: budget-app
type: Opaque
data:
  # These are base64 encoded values
  POSTGRES_DB: YXBw          # "app" in base64
  POSTGRES_USER: YXBw        # "app" in base64  
  POSTGRES_PASSWORD: c2VjdXJlcGFzc3dvcmQxMjM=  # "securepassword123" in base64
